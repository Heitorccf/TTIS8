clear all;
close all;
clc;

imgOriginal = imread('Imagens/cameraman.bmp');
[M, N]=size(imgOriginal);
c=imgOriginal;
c=double(c);
for i=2:M-1
    for j=2:N-1
        c(i, j)
    end
end
figure;
subplot(1, 2, 1), imshow(imgOriginal), title('Imagem com ruído');
subplot(1, 2, 2), imshow(imgProcessed), title('Imagem suavizada');